# 前端套餐时长模型升级与展示兼容阶段总结

- 日期：2026-05-12
- 仓库：`/Users/haixiang/Desktop/bingka1.0`
- 当前分支：`merge-upstream`

## 本轮目标

将前端商品时长展示与配置从只依赖 `unit_time` 的旧模型，升级为优先使用后端新增的：

- `duration_value`
- `duration_unit`

同时保留对历史 `unit_time` 数据的兼容回退，不为了“Week”继续扩时间单位枚举。

## 修改内容总览

### Admin 商品配置

- 商品表单新增：
  - `duration_value`
  - `duration_unit`
- 保留 `unit_time` 兼容字段同步写入，避免旧后端链路断裂。
- 编辑旧数据时，如果没有 `duration_*`，自动回退为：
  - `duration_value = 1`
  - `duration_unit = unit_time`
- 正式开放的时长单位收敛为：
  - `Day`
  - `Month`
  - `Year`
  - `NoLimit`
- 历史 `Hour / Minute` 商品仅做回显兼容，不再作为新建商品的正式选项开放。

### Admin 展示层

- 商品列表价格列不再直接展示旧 `unit_time`。
- 改为优先格式化显示新模型对应的人话时长，例如：
  - `7天`
  - `月卡`
  - `季卡`
  - `年卡`

### 用户前台展示

- 首页商品卡片已切换到新时长模型展示。
- 用户订阅列表已切换到新时长模型展示。
- 购买页、续费页、门户购买页时长选择器已切换。
- 账单区已切到新时长模型，相关支付/账单复用页面一并生效。

### 共享工具与类型

- 新增共享工具：
  - `packages/ui/src/utils/subscription-duration.ts`
- 统一封装：
  - 时长解析
  - 时长展示
  - 数量乘时长展示
- `packages/ui` 已补 `./utils` 导出，避免构建时报缺少导出入口。
- admin / user / common typings 已补：
  - `duration_value`
  - `duration_unit`

## 核心修改文件

### Admin

- `apps/admin/src/sections/product/subscribe-form.tsx`
- `apps/admin/src/sections/product/subscribe-table.tsx`
- `apps/admin/public/assets/locales/zh-CN/product.json`
- `apps/admin/public/assets/locales/en-US/product.json`

### User

- `apps/user/src/sections/main/product-showcase/content.tsx`
- `apps/user/src/sections/purchasing/content.tsx`
- `apps/user/src/sections/purchasing/index.tsx`
- `apps/user/src/sections/subscribe/billing.tsx`
- `apps/user/src/sections/subscribe/duration-selector.tsx`
- `apps/user/src/sections/subscribe/index.tsx`
- `apps/user/src/sections/subscribe/purchase.tsx`
- `apps/user/src/sections/subscribe/renewal.tsx`
- `apps/user/src/sections/user/dashboard/content.tsx`
- `apps/user/public/assets/locales/zh-CN/subscribe.json`
- `apps/user/public/assets/locales/en-US/subscribe.json`

### Shared / Typings

- `packages/ui/src/utils/subscription-duration.ts`
- `packages/ui/src/utils/index.ts`
- `packages/ui/package.json`
- `packages/ui/src/services/admin/typings.d.ts`
- `packages/ui/src/services/common/typings.d.ts`
- `packages/ui/src/services/user/typings.d.ts`

## 兼容策略

- 展示优先级：
  - `duration_value + duration_unit`
  - 回退 `unit_time`
- 提交优先写入：
  - `duration_value`
  - `duration_unit`
- 同时兼容保留：
  - `unit_time`
- 历史 `Hour / Minute` 商品不丢数据，但不继续作为正式模型扩展。

## 已完成验证

### 静态检查

- `bun run --cwd /Users/haixiang/Desktop/bingka1.0/apps/admin check`
  - 通过
- `bun run --cwd /Users/haixiang/Desktop/bingka1.0/apps/user check`
  - 通过
  - 仅存在项目内既有的大文件提示：
    - `public/assets/lotties/locations.json`
  - 该提示与本轮改动无关

### 构建验证

- `bun run --cwd /Users/haixiang/Desktop/bingka1.0/apps/admin build`
  - 通过
- `bun run --cwd /Users/haixiang/Desktop/bingka1.0/apps/user build`
  - 通过

### 额外说明

- 构建日志中仍有原项目既有的 chunk size warning
- `src/stores/global.tsx` 中已有的 `eval` warning 仍存在，但不是本轮改动引入

## 仍需人工验收

建议至少手工检查以下场景：

1. Admin 新建 `7天` 商品
2. Admin 编辑旧 `Month` 商品并确认回显
3. Admin 编辑历史 `Hour / Minute` 商品并确认不会丢值
4. 前台首页商品卡片时长展示
5. 用户订阅页商品卡片时长展示
6. 购买页时长选择器展示
7. 续费页时长选择器展示
8. 账单区时长与价格文案展示

## 当前风险与注意事项

1. `typings.d.ts` 为手工补字段  
   如果后续重新执行 OpenAPI 生成，需要同步把 `duration_value / duration_unit` 补回生成规则，否则会被覆盖。

2. 当前只是前端侧接入完成  
   正确展示与保存仍依赖后端已经提供 `duration_value / duration_unit`。

3. 当前策略是“兼容旧数据，不继续扩旧模型”  
   后续如果要彻底淡化 `unit_time`，需要前后端再做一轮统一收口。

## 当前结论

本轮已经完成前端套餐时长模型升级的主体工作：

- admin 配置已接入
- 用户展示已接入
- 共享格式化入口已接入
- 静态检查与构建都已通过

当前已达到可进入联调和人工验收阶段的状态。
